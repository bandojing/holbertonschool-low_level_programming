#include "holberton.h"

/**
 * print_last_digit  - Short description, single line
 *
(* a blank line
* @last: last is integer passed into the function
(* section header: Section description)*
* Return: returns last digit which equals int%10
*/

int print_last_digit(int last)
{

	return (last % 10);
}
