#include "holberton.h"
#include <stdio.h>
/**
 * _strlen - returns the length of a string
 * @s: s is a pointer to a string of chars
(* a blank line
* Description: uses for loop to cycle through *s string
* and retuns the number of characters in *s
(* section header: Section description)*
* Return: retuns an int called counter
*/
int _strlen(char *s)
{

	int counter;

	for (counter = 0; s[counter] != '\0'; counter++)
	{
	}
	return (counter);
}
