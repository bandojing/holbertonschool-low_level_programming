#include "holberton.h"
#include <stdio.h>
/**
 * _atoi - takes in pointer to s and returns 0
 * @s: pointer to char passed into _atoi
(* a blank line
* Description: takes in a pointer to s and returns 0
(* section header: Section description)*
* Return: returns 0
*/
int _atoi(char *s)
{
	return (0);
}
